[gd_resource type="ShaderMaterial" load_steps=3 format=2]

[ext_resource path="res://sprites/objects/babytexture1.png" type="Texture" id=1]

[sub_resource type="Shader" id=1]
code = "shader_type canvas_item;

uniform sampler2D Image;

void fragment(){
	vec4 data = texture(TEXTURE,UV);
	if (data.a < 1.0){
		discard;
	}
	COLOR = texture(Image,vec2(data.r*16.0,data.g*16.0));
	
}"

[resource]
shader = SubResource( 1 )
shader_param/Image = ExtResource( 1 )
